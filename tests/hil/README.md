# Hardware-in-the-loop Harness

- Connect reference anchors via Ethernet to ensure low jitter.
- Run `python scripts/validate.py --interface wlan0 --targets targets.yaml`.
- Logs persisted in `data/validation/runs.duckdb`.
- TODO: integrate motion capture ground truth.

