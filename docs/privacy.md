# Privacy

- Aether processes radio signal metadata only; no personal identifiers collected by default.
- Device identifiers (IP/MAC) stored only when explicitly logged for research.
- Users can disable persistence via config; recommend anonymization before export.

